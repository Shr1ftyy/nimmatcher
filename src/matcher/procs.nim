import types

# proc addOrderBook*(exchange: Exchange, orderbook: OrderBook) =
#     exchange.orderbooks[orderbook.ticker] = orderbook

# proc addStock(exchange: Exchange) =
#     var newOrderbook: OrderBook =  ()
#     exchange['']